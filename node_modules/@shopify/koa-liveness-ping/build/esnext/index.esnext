function ping() {
  return async function pingMiddleware(ctx, next) {
    if (ctx.path === '/ping') {
      ctx.status = 200;
      return;
    }

    await next();
  };
}

export { ping as default };
